# 👶 유어픽 (유치원·어린이집 비교) 개발자 가이드

안녕하세요! 이 프로젝트는 학부모님들이 집 근처의 유치원이나 어린이집을 쉽게 찾고, **최대 5개까지 골라 한 번에 비교**할 수 있게 돕는 서비스입니다.

---

## 🏗 시스템 작동 방식

1.  **데이터 준비**: `scripts/fetch-data.js`가 정부 공공 데이터에서 유치원 정보를 가져와서 `public/data/`에 큰 JSON 파일로 저장해 둡니다.
2.  **빠른 검색**: 사용자가 지도나 검색창을 건드리면 서버에 요청하는 대신, 이미 가지고 있는 JSON 파일에서 즉시 결과를 찾아보여줍니다. (엄청 빠릅니다!)
3.  **비교함**: 맘에 드는 곳을 고르면 **Zustand** 스토어에 저장되고, 비교 페이지에서 항목별로 대조해 볼 수 있습니다.

---

## 🛠 주요 명령어 및 작업 안내

- `npm run prebuild`: **매우 중요합니다.** 새로운 데이터를 가져오거나 가공해서 앱에 넣고 싶을 때 빌드 전에 실행해야 합니다.
- `npm run dev`: 화면 디자인을 바꾸거나 지도/검색 로직을 고칠 때 사용하세요.

---

## 💡 개발자를 위한 팁

### 데이터 갱신 (Fetch & Augment)
- `scripts/fetch-data.js`: 데이터를 새로 긁어옵니다.
- `scripts/augment-data.js`: 긁어온 데이터에 추가 정보(예: 운영시간 상세 등)를 덧붙여서 더 풍부하게 만듭니다.

### 비교 기능 수정
- 사용자가 고른 기관 리스트는 브라우저를 껐다 켜도 남아 있어야 합니다. 이를 위해 `zustand/persist` 기술이 사용되고 있습니다.

---

## 🚨 문제 해결 (Troubleshooting)

**Q: 화면에 기관 정보가 하나도 안 나와요.**
- A: `npm run prebuild`를 실행하지 않아서 `public/data/institutions.json` 파일이 비어 있거나 없을 수 있습니다.

**Q: Cafe24 배포 후에 경로가 꼬여요.**
- A: 이 프로젝트는 `domain.com/yourpick` 같은 주소에서 동작하도록 설정되어 있습니다. `next.config.ts`의 `basePath` 설정을 확인하세요.

---

## 📁 주요 구성 요소
- `scripts/`: 공공 데이터를 가져와서 요리하는 파일들의 모음.
- `src/stores/`: '비교함'에 담긴 기관들을 관리하는 데이터 박스.
- `src/app/`: 내 주변 기관 찾기 및 비교 메인 UI.
